# need to use mask's in attn block for pad tokens
# some tokens are not making it to the training, use pad tokens for that, make drop last = False in both dataloader and sampler
# need to implement pad tokens in dataset.py
# need to check the sample output
# need to download hello swag dataset for eval
# need to use seperate eval dataset for eval checking

data allocation, data labeling&cleaning, hyperparameters strategy, scaling law, curriculum learning, annealing, training optimization(deepspeed&megatron), training experiments, loss tracking&logging